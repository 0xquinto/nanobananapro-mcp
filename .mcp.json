{
  "mcpServers": {
    "nanobananapro": {
      "command": "uv",
      "args": ["run", "python", "-m", "nanobananapro_mcp"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
